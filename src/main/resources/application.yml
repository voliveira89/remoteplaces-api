spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/rp?allowMultiQueries=true
    username: rpuser
    password: rppassword
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB103Dialect
        temp:
          use_jdbc_metadata_defaults: false
